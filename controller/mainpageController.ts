import { Request, Response, NextFunction } from "express";


export = {
    calender :async (req: Request, res: Response, next: NextFunction) => {
        
    },

    todolist :async (req: Request, res: Response, next: NextFunction) => {
        
    }
}